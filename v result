<script>
    import { link } from "svelte-routing";

    // Svelte routing automatically injects location state here
    export let location;
   
    // Fallback data structure if someone navigates here directly without capturing an image
    $: result = location.state?.result || null;
    $: savedFile = location.state?.savedFile || "";
</script>

<main>
    <div class="card result-card">
        <h1>📊 Diagnostic Analysis Report</h1>
        <hr />

        {#if result}
            <div class="meta-section">
                <p><strong>Target Patient ID:</strong> User #{result.user_id}</p>
                <p><strong>Timestamp:</strong> {new Date().toLocaleString()}</p>
                <p><strong>Archived Snapshot:</strong> <span class="path">{result.saved_file || savedFile}</span></p>
            </div>

            <div class="metrics-grid">
                <div class="metric-box status-{result.status}">
                    <span class="label">ANALYSIS STATUS</span>
                    <span class="value">{result.status.toUpperCase()}</span>
                </div>
                <div class="metric-box grade-box">
                    <span class="label">MODEL PREDICTION</span>
                    <span class="value">{result.grade}</span>
                </div>
            </div>

            <h3>Raw ONNX Metadata payload:</h3>
            <div class="raw-json">
                <pre>{JSON.stringify(result, null, 2)}</pre>
            </div>
        {:else}
            <div class="error-state">
                <p>⚠️ No active analysis dataset found. Please capture a live frame first.</p>
            </div>
        {/if}

        <a href="/" use:link class="back-btn">← Return to Live Stream</a>
    </div>
</main>

<style>
    main {
        display: flex;
        justify-content: center;
        align-items: center;
        padding: 40px 20px;
    }
    .card {
        background: #2a2a2a;
        border-radius: 8px;
        padding: 30px;
        box-shadow: 0 4px 15px rgba(0,0,0,0.3);
        max-width: 600px;
        width: 100%;
    }
    hr {
        border: 0;
        height: 1px;
        background: #444;
        margin: 20px 0;
    }
    .meta-section {
        background: #1e1e1e;
        padding: 15px;
        border-radius: 6px;
        font-size: 14px;
        margin-bottom: 25px;
    }
    .path {
        font-family: monospace;
        color: #007acc;
    }
    .metrics-grid {
        display: flex;
        gap: 15px;
        margin-bottom: 25px;
    }
    .metric-box {
        flex: 1;
        padding: 15px;
        border-radius: 6px;
        display: flex;
        flex-direction: column;
        align-items: center;
    }
    .status-success { background: #1b4332; border: 1px solid #2d6a4f; color: #52b788; }
    .status-failed { background: #4c1d1d; border: 1px solid #7a2222; color: #e63946; }
    .grade-box { background: #1d3557; border: 1px solid #457b9d; color: #a8dadc; }
    .label { font-size: 11px; letter-spacing: 1px; color: #aaa; margin-bottom: 5px; }
    .value { font-size: 20px; font-weight: bold; }
    .raw-json {
        background: #111;
        padding: 15px;
        border-radius: 4px;
        overflow-x: auto;
    }
    pre { margin: 0; font-family: monospace; color: #39b54a; font-size: 13px; }
    .back-btn {
        display: inline-block;
        margin-top: 25px;
        color: #007acc;
        text-decoration: none;
        font-weight: bold;
    }
    .back-btn:hover { text-decoration: underline; }
</style>
